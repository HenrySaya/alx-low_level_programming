#include "main.h"

/**
 * print_numbers - print the numbers, from 0 to 9
 *
 * Return: returns (0)
 */

void print_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
		_putchar(i);
	_putchar(10);
	return (0);
}
